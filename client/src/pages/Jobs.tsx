import styled from 'styled-components';

type Props = {};

function Jobs({}: Props) {
  return <Wrapper>Jobs</Wrapper>;
}

export default Jobs;

const Wrapper = styled.div`
  margin-top: 4rem;
  max-width: 1320px;
  margin: 4rem auto;
  padding: 0 2rem;
`;
