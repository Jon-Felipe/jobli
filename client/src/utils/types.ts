import { IconType } from 'react-icons/lib';

export type JobCategory = {
  id: number;
  icon: IconType;
  title: string;
  text: string;
};

export type Services = {
  id: number;
  icon: IconType;
  title: string;
  text: string;
};
